/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-svelte'],
  printWidth: 100,
  singleQuote: true,
  semi: true,
  trailingComma: 'none'
};

export default config;
